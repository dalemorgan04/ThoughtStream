@using Tasks.ViewModels.Tasks
@model TasksViewModel

@{
    ViewBag.Heading = "Tasks";
}

@section urls{
    <script>
        var urlsPubsub = {
            getAddTask: '@Url.Action("GetAsideAddTab", "Tasks")',
            getEditTask: '@Url.Action("GetAsideEditTab", "Tasks")',
            getTasksTable: '@Url.Action("GetTasksTable", "Tasks")',
            saveTask: '@Url.Action("Create", "Tasks")'
            }
    </script>
}

<h1>@ViewBag.Heading</h1>

<div class="input-group-btn">
    <button id="btnShowAddTask" type="submit" value="Create" class="btn btn-default">New Task</button>
</div>

<div id="addTaskContainer">
</div>

<div id="taskResultsTable">
    @Html.Partial("_TasksTable", Model)
</div>